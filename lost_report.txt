比较 annotated(b4a5c8d79) vs anno-rebuild(工作树)：用户注释丢失报告
============================================================
在 annotated 中识别到的用户注释行总数: 13349
在 anno-rebuild 中丢失的注释行数: 846  (中文/含CJK: 646, 非中文/英文: 200)
受影响文件数: 57 / 180
============================================================

## src/http/ngx_http_request.h  (用户注释 440 行; 丢中文 34 行, 丢英文 41 行)
    - [中] //如果这个请求是子请求，则该时间是子请求的生成时间；
    - [中] *   ngx_http_get_module_ctx(r, module) — Returns the module's context
    - [中] //如果这个请求是用户发来的请求，则是在建立起TCP连接后，第一次接收到可读事件时的时间
    - [中] //如ngx_buf_t实际数据不可写，则设置此标识会导致buf被重新复制一份并标记可写
    - [中] //是一个链表，每个节点是一个子请求产生的响应，用于保证子请求有序向客户端转发，参考ngx_http_postpone_filter_module模块
    - [中] //而 main则标识一系列相关的派生子请求的原始请求， contains a link to the main request in a group of requests.
    - [中] * The value is inferred from the HTTP version and the value of the “Connection” header.
    - [中] //表示使用rewrite重写 URL的次数。因为目前最多可以更改10次，所以uri_changes初始化为11，
    - [中] //标志位，为 1时表示请求的当前状态是在做内部跳转。(internal request) 或者子请求
    - [中] //表示这个请求的内存池，在ngx_http_free_request方法中销毁。
    - [中] //而每重写URL一次就把uri_changes减1，一旦uri_changes等于 0，则向用户返回失败
    - [中] * 当ngx_http_upstream_conf_t配置结构体中的buffering标志位为1时，将开启更多的内存和磁盘文件用于缓存上游的响应包体，
    - [中] // 阻塞标志位，目前仅由 aio使用
    - [中] * start_sec, start_msec — Time point when the request was created, used for tracking request duration
    - [中] //*当前请求既可能是用户发来的请求，也可能是派生出的子请求，
    - [中] //一般可通过 main和当前请求的地址是否相等来判断当前请求是否为用户发来的原始请求
    - [中] // 标志表明只能将单个连续的输出数据范围发送到客户端。通常在发送数据流时设置此标志，例如从代理服务器发送，整个响应在不在一个缓冲区中.参考 range_filter模块
    - [中] //会把解析完的每一个 HTTP头部加入到 headers_in的 headers链表中，同时会构造 headers_in中的其他成员
    - [中] //标志位，为 1时表示当前请求是 keepalive请求。从HTTP版本和“Connection”标头的值推断的出来
    - [中] * 这意味上游网速更快；当buffering为0时，将使用固定大小的缓冲区（就是上面介绍的buffer缓冲区）来转发响应包体
    - [中] * 当subrequest_in_memory为0时，upstream会转发响应包体。
    - [中] //ngx_http_process_request_headers方法在接收、解析完 HTTP请求的头部后，
    - [中] //它与 ngx_connection_t中的内存池意义不同，当请求释放时，
    - [中] * headers_in, headers_out — Input and output HTTP headers objects.
    - [中] //如果是子请求，结束时会调用此字段里的handler。 参考：ngx_http_finalize_request
    - [中] //避免请求体临时文件被upstream模块清理
    - [中] //每个子请求结束时的回调函数，组成一个链表
    - [中] *   ngx_http_set_ctx(r, c, module) — Sets c as the module's context
    - [中] //指向每个HTTP模块的自定义上下文结构体的数组. 数组的元素个数为ngx_http_max_module
    - [中] //save the entire client request body in a single buffer. 用于访问 $request_body 场景
    - [中] //TCP连接可能并没有关闭，这时请求的内存池会销毁，但 ngx_connection_t的内存池并不会销毁
    - [中] //为1表示要在内存中处理子请求的响应。数据不能超过subrequest_output_buffer_size设置的大小
    - [中] * upstream机制中，subrequest_in_memory标志位为1时, 即upstream不转发响应包体到下游，由HTTP模块实现的input_filter方法处理包体
    - [中] //输出缓冲区和子请求的列表，按照它们发送和创建的顺序。当子请求创建时，该列表由postpone过滤器使用，以提供一致的请求输出
    - [英] * Both objects contain the headers field of type ngx_list_t for keeping the raw list of headers.
    - [英] * Each module of type NGX_HTTP_MODULE can store any value (normally, a pointer to a structure) in the request
    - [英] * Pointer to a main request object.
    - [英] * This is used by filters which may change output directly in the buffers where it's sent.
    - [英] * This object is created to process a client HTTP request, as opposed to subrequests, which are created to perform a specific subtask within the main request
    - [英] *  Number of URI changes remaining for the request.
    - [英] not be removed by the upstream module because it may be used later by a
    - [英] * Currently, this value is increased by pending AIO operations (POSIX AIO and thread operations) and active cache lock
    - [英] * Counter of blocks held on the request. While this value is non-zero, the request cannot be terminated.
    - [英] * The total number of times a request can change its URI is limited by the NGX_HTTP_MAX_URI_CHANGES constant.
    - [英] *  Pointer to a handler with the context to be called when a subrequest gets finalized. Unused for main requests.
    - [英] * The value is stored in the ctx array at the module's ctx_index position
    - [英] * Flag indicating that the current request is internal.
    - [英] * To enter the internal state, a request must pass through an internal redirect or be a subrequest.
    - [英] //https://nginx.org/en/docs/http/ngx_http_core_module.html#client_body_in_single_buffer
    - [英] * For allocations that need to be available throughout the client connection's lifetime, use ngx_connection_t's pool instead.
    - [英] **/
    - [英] * Request pool. The request object itself is allocated in this pool, which is destroyed when the request is deleted.
    - [英] * Array of HTTP module contexts
    - [英] //https://nginx.org/en/docs/http/ngx_http_core_module.html#subrequest_output_buffer_size
    - [英] cache update subrequest is started to prevent the request body file removal
    - [英] * Upstream: keep request body file from removal if requested.
    - [英] * The following fields of the request determine how the request body is read:
    - [英] * In addition to that, specific headers are available for getting and setting as separate fields, for example content_length_n, status etc
    - [英] subrequest. The flag is set by the SSI (ticket #585), addition and slice
    - [英] file.
    - [英] // Read the body to a single memory buffer
    - [英] * Flag indicating whether client connection keepalive is supported.
    - [英] * With each change the value is decremented until it reaches zero, at which time an error is generated.
    - [英] * Flag requesting that the request output be produced in temporary buffers, but not in readonly memory buffers or file buffers.
    - [英] *  */
    - [英] * Flag indicating that only a single continuous range of output data can be sent to the client.
    - [英] The new request flag "preserve_body" indicates that the request body file should
    - [英] //https://tengine.taobao.org/book/chapter_12.html#subrequest-99
    - [英] * List of output buffers and subrequests, in the order in which they are sent and created.
    - [英] * This flag is usually set when sending a stream of data, for example from a proxied server, and the entire response is not available in one buffer.
    - [英] * Rewrites and internal redirects to normal or named locations are considered URI changes.
    - [英] * Internal requests are allowed to enter internal locations.
    - [英] after an internal redirect. Only the main request is now allowed to remove the
    - [英] * The list is used by the postpone filter to provide consistent request output when parts of it are created by subrequests.
    - [英] modules. Additionally, it is also set by the upstream module when a background

## src/http/ngx_http.c  (用户注释 460 行; 丢中文 35 行, 丢英文 34 行)
    - [中] * 则进行server{}块下create_loc_conf 生成的结构体与嵌套location{}块配置项生成的结构体进行merge_loc_conf操作；
    - [中] 并发送序列化之后的响应头部
    - [中] * 这里合并http{}块下main、server{}块下srv级别与server相关的配置项结构；
    - [中] * 则将当前HTTP模块在http{}块下由create_srv_conf 生成的结构体
    - [中] * saved.srv_conf[ctx_index]表示当前HTTP模块在http{}块下由create_srv_conf方法创建的结构体；
    - [中] * 解析完成http{}块内的所有指令后（包括server{}、location{}块的解析），
    - [中] //遍历http{}块下的所有server{}块, 构建由location块构造的静态二叉平衡查找树
    - [中] * 因为location{}继续内嵌location{}
    - [中] * 与遍历每个server{}块由create_srv_conf生成的配置项结构体进行merge_srv_conf合并操作；
    - [中] //在 10.2.3节中曾经说过，它的locations成员将以双向链表的形式关联到所有当前 server{}块下的 location块
    - [中] //遍历 locations双向链表
    - [中] * 则进行http{}块下create_srv_conf 生成的结构体与遍历server{}块配置项生成的结构体进行merge_srv_conf操作；
    - [中] * cscfp[s]->ctx->srv_conf[ctx_index]表示当前HTTP模块在server{}块下由create_srv_conf方法创建的结构体；
    - [中] * 进行下面的程序
    - [中] * 递归调用该函数；
    - [中] //server块下 ngx_http_core_module模块使用 create_loc_conf方法产生的 ngx_http_core_loc_conf_t结构体，
    - [中] * 使用方法
    - [中] //初始化listen 端口号 ip地址 服务器等监听信息
    - [中] // 调用 ngx_http_merge_servers方法合并 ngx_modules[m]模块
    - [中] * 响应体过滤链表的顺序
    - [中] //1. 构造用于初始化hash表的结构体 headers_in
    - [中] * 则进行location{}块下create_loc_conf 生成的结构体与嵌套location{}块配置项生成的结构体进行merge_loc_conf操作；
    - [中] 除了最后一个模块是真正发送响应头部给客户端之外，其他模块都只是对响应头部进行修改
    - [中] // 链表的最后一个模块是ngx_http_header_filter_module
    - [中] // 如果当前 HTTP模块实现了 merge_srv_conf，则再调用合并方法
    - [中] * 响应头过滤链表的顺序:
    - [中] * headers_in 初始有32个ngx_hash_key_t元素
    - [中] // 过滤链表头指针，过滤header
    - [中] /* 设置server与监听端口的关系，并设置新连接事件的处理方法 */
    - [中] //cmcf是全局唯一的代表http{}块的配置结构
    - [中] * 若定义了merge_srv_conf 方法；
    - [中] ngx_http_header_filter_module，提供的处理方法ngx_http_header_filter 根据请求结构体ngx_http_request_t 中的 header_out 成员序列化字符流，
    - [中] /* 以下是监听端口管理的内容 */
    - [中] //因为 location{}中可以继续嵌套 location{}配置块，所以是可以继续合并的
    - [中] /* 合并当前HTTP模块不同级别的配置项结构 */
    - [英] * ngx_http_next_header_filter = ngx_http_top_header_filter;
    - [英] +----------+-----------------+
    - [英] |ngx_http_userid_filter|
    - [英] +----------+-------------------+
    - [英] |ngx_http_postpone_filter|
    - [英] +---------------------+
    - [英] +----------+------------+
    - [英] +----------+-----------+
    - [英] +----------+---------------+
    - [英] |ngx_http_copy_filter|
    - [英] |ngx_http_gzip_header_filter|
    - [英] |ngx_http_charset_header_filter|
    - [英] +----------+-------------+
    - [英] +----------+----------------+
    - [英] |ngx_http_ssi_header_filter|
    - [英] *+----------------------------+
    - [英] +----------------------+
    - [英] +----------+--------------+
    - [英] |ngx_http_chunked_header_filter|
    - [英] |ngx_http_headers_filter|
    - [英] * +--------------------------+
    - [英] |ngx_http_chunked_body_filter|
    - [英] * ngx_http_next_body_filter = ngx_http_top_body_filter;
    - [英] * ngx_http_top_body_filter = ngx_http_myfilter_body_filter;
    - [英] |ngx_http_range_header_filter|
    - [英] |ngx_http_write_filter|
    - [英] |ngx_http_gzip_body_filter|
    - [英] |ngx_http_charset_body_filter|
    - [英] +----------+---------+
    - [英] |ngx_http_not_modified_filter|
    - [英] |ngx_http_ssi_body_filter|
    - [英] |ngx_http_header_filter|
    - [英] * ngx_http_top_header_filter = ngx_http_myfilter_header_filter;
    - [英] |ngx_http_range_body_filter|

## src/http/ngx_http_request.c  (用户注释 644 行; 丢中文 52 行, 丢英文 10 行)
    - [中] * 则需要把当前连接的读事件从定时器机制中删除；
    - [中] * 2、对HTTP请求头部协议版本进行检查，例如http1.1版本，host头部不能为空，否则会返回400 Bad Request错误；
    - [中] // done，例如调用read body,因为count已经增加，所以不会关闭请求
    - [中] * 由于现在不需要再接收HTTP请求头部超时问题，
    - [中] /* 为当前连接创建一个请求结构体ngx_http_request_t */
    - [中] //找到父请求的posted_requests链表末端
    - [中] // header_name_start 指向一个key value请求头开始
    - [中] * 若当前连接的读事件未准备就绪，
    - [中] * 则将其添加到定时器事件机制，并注册到epoll事件机制中；
    - [中] //如果readEvent仍然在定时器中，将其删除
    - [中] // 或者复制一个key value请求头到新分配的内存
    - [中] // 复制整个请求行到新分配的内存
    - [中] * 参数rc就非常复杂了，它既可能是NGX_OK、NGX_ERROR、NGX_AGAIN、 NGX_DONE、NGX_DECLINED这种系统定义的返回值，又可能是类似 NGX_HTTP_REQUEST_TIME_OUT这样的HTTP响应码，因此，ngx_http_finalize_request方法 的流程异常复杂
    - [中] // 并不一定会真正结束，大部分情况下只是暂时停止处理，等待epoll事件发生
    - [中] * 请求行和一个请求头都不能超过一个最大缓冲区的大小
    - [中] * 是开发HTTP模块时最常使用的结束请求方法
    - [中] //读事件是否已经超时，超时时间仍然是 client_header_timeout
    - [中] * 则将其从定时器机制中删除，0表示不在定时器机制中；
    - [中] * 接着继续解析下一行请求头部；
    - [中] * 若当前解析出来的一行请求头部是合法的，表示成功解析出该行请求头部，
    - [中] //Http 1.1的pipeline请求，如果前面的请求分配的large buf，那么后面的请求会继承使用这个large buf分配的空间，当large buf 不够了再去主动分配large buf
    - [中] * 将该行请求头部保存在当前请求的headers_in的headers链表中；
    - [中] /* 拷贝旧缓冲区中不完整的请求头 */
    - [中] //添加读取header超时定时器
    - [中] // 参数rc决定了函数的逻辑，在content阶段就是handler的返回值
    - [中] /* 如果一个大缓冲区还装不下请求行或者一个请求头，则返回错误 */
    - [中] /* 若当前读事件超时，则进行相应地处理，并关闭当前请求 */
    - [中] * 这样做的目的也是为了给该连接的下一次请求重用这个缓冲区，另外如果客户端发过来的请求头大于1024个字节，nginx会重新分配更大的缓存区，
    - [中] // request_start 指向请求求行开始
    - [中] * 分配一个缓冲区用来保存它的请求头
    - [中] // 如果是解析请求行分配的，则需要重新修改指向解析请求行后的指针
    - [中] /* 在当前连接上开始接收HTTP请求数据 */
    - [中] * nginx用来保存请求头的缓冲区是在该请求所在连接的内存池中分配，而且会将地址保存一份在连接的buffer字段中，
    - [中] * 默认用于大请求的头的缓冲区最大为8K，最多4个，这2个值可以用large_client_header_buffers指令设置，
    - [中] //子请求，需要跳到它的父请求上，激活父请求继续向下执行
    - [中] * 1、根据HTTP头部的host字段，调用ngx_http_find_virtual_server查找虚拟主机的配置块；
    - [中] //主要逻辑是进行请求头格式校验，是否符合http协议规范
    - [中] //r->post_subrequest在 ngx_http_subrequest()方法中设置
    - [中] //此时将c->data设置为ngx_http_request_t结构体
    - [中] /* 修改相应的指针指向新缓冲区 */
    - [中] * 参数r就是当前请求，它可能是派生出的子请求，也可能是客户端发来的原始请求。
    - [中] // 重要函数，以“适当”的方式“结束”请求
    - [中] // 请求行太大 或 请求头的一个key value太大
    - [中] // 调用ngx_http_finalize_connection，检查请求相关的异步事件，尝试关闭请求
    - [中] * timer_set为1表示读事件已添加到定时器机制中，
    - [中] //接收数据,最多接收buffer的剩余空间大小的字节
    - [中] * 调用该函数主要目的有两个：
    - [中] /* 保存请求行或者请求头在旧缓冲区中的起始地址 */
    - [中] //如果是pipeline的请求就直接用hc->busy就可以了
    - [中] /* 如果当前请求是某个原始请求的一个子请求，检查它是否有回调handler处理函数，若存在则执行 */
    - [中] /* 该子请求还有未处理完的数据或者子请求 */
    - [中] //添加到headers_in链表中
    - [英] * A request is also finalized in case of an error or if a standard HTTP response code needs to be returned to the client
    - [英] * Other codes are considered successful finalization codes and might activate the request writer to finish sending the response body. Once the body is completely sent, the request count is decremented. If it reaches zero, the request is destroyed, but the client connection can still be used for other requests. If count is positive, there are unfinished activities within the request, which will be finalized at a later point.
    - [英] * It is usually finalized by the content handler after all output buffers are sent to the filter chain.
    - [英] * If it is, the ngx_http_finalize_request(r, rc) function automatically installs a special handler ngx_http_writer(r) to finish sending the output
    - [英] * NGX_DONE - Fast finalization. Decrement the request count and destroy the request if it reaches zero. The client connection can be used for more requests after the current request is destroyed.
    - [英] * An HTTP request is finalized by calling the function ngx_http_finalize_request(r, rc).
    - [英] * NGX_ERROR, NGX_HTTP_REQUEST_TIME_OUT (408), NGX_HTTP_CLIENT_CLOSED_REQUEST (499) - Error finalization. Terminate the request as soon as possible and close the client connection.
    - [英] * https://nginx.org/en/docs/dev/development_guide.html#http_request_finalization
    - [英] *  At this point all of the output might not be sent to the client, with some of it remaining buffered somewhere along the filter chain
    - [英] * NGX_HTTP_CREATED (201), NGX_HTTP_NO_CONTENT (204), codes greater than or equal to NGX_HTTP_SPECIAL_RESPONSE (300) - Special response finalization. For these values nginx either sends to the client a default response page for the code or performs the internal redirect to an error_page location if that is configured for the code.

## src/http/ngx_http_core_module.h  (用户注释 346 行; 丢中文 26 行, 丢英文 24 行)
    - [中] * 在 HTTP 协议中，Vary 响应头用于告知缓存机制（如浏览器缓存、CDN 或代理服务器），在判断是否直接使用缓存内容时，
    - [中] //这一阶段是用于在 rewrite重写 URL后，防止错误的 nginx.conf配置导致死循环（递归地修改 URI）
    - [中] //用于让HTTP模块判断是否允许这个请求访问 Nginx服务器
    - [中] //进入该阶段表明该请求的响应已经发送到系统发送缓冲区, 在ngx_http_free_request中执行。具体的执行的函数为ngx_http_log_request
    - [中] //使得每个 HTTP请求处理完毕后会记录 access_log访问日志
    - [中] //并根据方法的返回值决定：当前阶段已经完全结束了吗？下次要执行的回调方法是哪一个？
    - [中] //Enables or disables inserting the “Vary: Accept-Encoding” response header field if the directives gzip, gzip_static, or gunzip are active.
    - [中] //如果一个请求超过10次重定向 ,就认为进入了rewrite死循环，这时在 NGX_HTTP_POST_REWRITE_PHASE阶段就会向用户返回 500，表示服务器内部错误
    - [中] //ngx_http_phase_handler_t结构体仅表示处理阶段中的一个处理方法
    - [中] // 用于处理 HTTP请求内容的阶段，这是大部分 HTTP模块介入的阶段
    - [中] //这一阶段仅由 ngx_http_core_module模块处理。目前，控制死循环的方式很简单，首先检查 rewrite的次数，
    - [中] //因此，这个阶段实际上用于给NGX_HTTP_ACCESS_PHASE阶段收尾
    - [中] * 在处理到某一个 HTTP阶段时， HTTP框架将会在 checker方法已实现的前提下首先调用 checker方法来处理请求，
    - [中] * 因此，事实上所有的 checker方法都是由框架中的 ngx_http_core_module模块实现的，且普通的 HTTP模块无法重定义checker方法
    - [中] //这4个checker方法的主要任务在于，根据phase_handler执行某个HTTP模块实现的回调方法，
    - [中] //（实际就是 NGX_HTTP_FORBIDDEN或者 NGX_HTTP_UNAUTHORIZED），这里将负责向用户发送拒绝服务的错误响应。
    - [中] //处理 NGX_HTTP_ACCESS_PHASE阶段决定请求的访问权限前， HTTP模块可以介入的处理阶段
    - [中] //处理完请求后记录日志的阶段。例如，ngx_http_log_module模块就在这个阶段中加入了一个 handler处理方法，
    - [中] * 而不会直接调用任何阶段中的handler方法，只有在 checker方法中才会去调用 handler方法。
    - [中] //该指令用于设定是否相应数据包添加Vary：Accept-Enconding HTTP头（header）。需要注意的而是，由于bug的原因，如果设置添加该头，会导致IE4~6不缓存内容
    - [中] * 需要考虑哪些请求头的变化。它的作用是确保缓存的响应内容能够正确匹配后续请求的上下文环境（如不同语言、编码格式等），
    - [中] //在 NGX_HTTP_ACCESS_PHASE阶段中，当 HTTP模块的 handler处理函数返回不允许访问的错误码时
    - [中] //究竟是立刻执行下一个回调方法还是先把控制权交还给epoll
    - [中] * 避免因缓存 “一刀切” 导致的内容不匹配问题
    - [中] //根据请求的 URI寻找匹配的 location表达式
    - [中] //寻找location配置阶段，该阶段使用重写之后的uri来查找对应的location, 该阶段可能会被执行多次，因为也可能有location级别的重写指令
    - [英] * It's also possible to set content handlers on a per-location basis
    - [英] * */
    - [英] * The standard nginx modules ngx_http_limit_conn_module and ngx_http_limit_req_module register their handlers at this phase.
    - [英] * They are called sequentially until one of them produces the output.
    - [英] * Special phase where the satisfy any directive is processed.
    - [英] * No additional handlers can be registered at this phase
    - [英] * Currently, only the ngx_http_log_module registers its handler at this stage for access logging.
    - [英] * If some access phase handlers denied access and none explicitly allowed it, the request is finalized.
    - [英] * Multiple nginx standard modules register their handlers at this phase, including ngx_http_index_module or ngx_http_static_module.
    - [英] * Log phase handlers are called at the very end of request processing, right before freeing the request
    - [英] * and any module requesting a location configuration receives the configuration for the default server location.
    - [英] * A common phase for different types of handlers, not associated with access control.
    - [英] * Phase where it is verified that the client is authorized to make the request
    - [英] * By default the client must pass the authorization check of all handlers registered at this phase for the request to continue to the next phase
    - [英] * If the ngx_http_core_module's location configuration has handler set, it is called as the content handler and the handlers installed at this phase are ignored
    - [英] * Before this phase, the default location for the relevant virtual server is assigned to the request,
    - [英] * Phase where the response is normally generated.
    - [英] * This phase assigns a new location to the request. No additional handlers can be registered at this phase
    - [英] * Special phase where the request is redirected to a new location if its URI changed during a rewrite.
    - [英] * The satisfy directive, can be used to permit processing to continue if any of the phase handlers authorizes the client
    - [英] //https://nginx.org/en/docs/http/ngx_http_gzip_module.html#gzip_vary
    - [英] * Special phase where a location is chosen based on the request URI.
    - [英] * Phase where request logging is performed.
    - [英] * This is implemented by the request going through the NGX_HTTP_FIND_CONFIG_PHASE again.

## src/http/ngx_http_upstream.h  (用户注释 310 行; 丢中文 35 行, 丢英文 1 行)
    - [中] * upstream有3种处理上游响应包体的方式，但HTTP模块如何告诉upstream使用哪一种方式处理上游的响应包体呢？
    - [中] //处理包体的方法，其中 data参数用于传递用户数据结构，它实际上就是下面的 input_filter_ctx指针，
    - [中] * 当 Nginx与上游间的网速远大于Nginx与下游客户端间的网速时，让Nginx开辟更多的内存甚至使用磁盘文件来缓存上游的响应包体，
    - [中] //而 bytes表示本次接收到的包体长度。返回 NGX_ERROR时表示处理包体错误，请求需要结束，否则都将继续upstream流程
    - [中] //如果选择重定义input_filter_init/input_filter方法，意味着对upstream模块的默认的实现不满意
    - [中] //因为该方法发送请求时会自动把未发送完的 request_bufs链表记录下来，为了防止反复发送重复请求，
    - [中] //表示 upstream机制已经向上游服务器发送了全部或者部分的请求。
    - [中] //事实上，这个标志位更多的是为了使用 ngx_output_chain方法发送请求，
    - [中] //request_sent表示是否已经向上游服务器发送了请求，当 request_sent为 1时，
    - [中] //必须有request_sent标志位记录是否调用过 ngx_output_chain方法
    - [中] * 当buffering为0时，将使用固定大小的缓冲区（就是上面介绍的buffer缓冲区）来转发响应包体
    - [中] * 这是有意义的，它可以减轻上游服务器的并发压力。当 buffering为0时，表示只使用上面的这一个buffer缓冲区来向下游转发响应包体
    - [中] * upstream机制提供的8个回调：必需（create_request/process_header/finalize_request)
    - [中] //如果这个回调函数为NULL，则会使用nginx默认提供的ngx_http_upstream_non_buffered_filter
    - [中] //upstream提供了3种处理上游服务器包体的方式，包括交由HTTP模块使用input_filter回调方法直接处理包体、
    - [中] * 当subrequest_in_memory为0时，upstream会转发响应包体。当ngx_http_upstream_conf_t配置结构体中的buffering标志位为1时，
    - [中] * a)在使用 process_header方法解析上游响应的包头时， buffer中将会保存完整的响应包头；
    - [中] * 2)当无须在上、下游间转发响应时，并不想等待接收完全部的上游响应后才开始处理请求。可以每解析完一部分包体，就释放一些内存
    - [中] * buffer成员存储接收自上游服务器发来的响应内容，由于它会被复用，所以具有下列多种意义：
    - [中] * 会在以下场景决定重新实现input_filter方法:
    - [中] *    即upstream不转发响应包体到下游，由HTTP模块实现的input_filter方法处理包体；
    - [中] *    将开启更多的内存和磁盘文件用于缓存上游的响应包体，这意味上游网速更快；
    - [中] /*与reinit_request回调方法有关*/
    - [中] //接收上游服务器响应包头的缓冲区，在不需要把响应直接转发给客户端，或者 buffering标志位为 0的情况下转发包体时，
    - [中] * 非必需(input_filter_init/input_filter/reinit_filter/reinit_request/abort_request/rewrite_request)
    - [中] * 当请求的ngx_http_request_t结构体中subrequest_in_memory标志位为1时，将采用第1种方式，
    - [中] * 在向客户端转发上游服务器的包体时才有用。当buffering为1时，表示使用多个缓冲区以及磁盘文件来转发上游的响应包体。
    - [中] //向下游转发上游的响应包体时，是否开启更大的内存及临时磁盘文件用于缓存来不及发送到下游的响应包体
    - [中] * d)当upstream并不用于转发上游包体时，buffer会被用于反复接收上游的包体， HTTP模块实现的input_filter方法需要关注它
    - [中] //接收包体的缓冲区仍然使用 buffer。注意，如果没有自定义 input_filter方法处理包体，将会使用 buffer存储全部的包体，
    - [中] //以固定缓冲区转发包体、以多个缓冲加磁盘文件的方式转发包体等。如果http模块没有实现这两个方法，nginx会使用预置的方法(nginx准备了3对input_filter_init/input_filter方法， 对应3种处理上游服务器包体的方式)
    - [中] * b)当下面的 buffering成员为1，而且此时 upstream是向下游转发上游的包体时， buffer没有意义；
    - [中] * c)当buffering标志位为 0时， buffer缓冲区会被用于反复地接收上游的包体，进而向下游转发；
    - [中] * 1)在转发上游响应到下游的同时，需要做一些特殊处理 如ngx_http_memcached_module模块, 需协议转换。不是完全地/纯粹地透传TCP流
    - [中] //这时 buffer必须足够大！它的大小由 ngx_http_upstream_conf_t配置结构体中的 buffer_size成员决定
    - [英] * https://nginx.org/en/docs/http/ngx_http_proxy_module.html#proxy_buffering

## src/core/nginx.c  (用户注释 150 行; 丢中文 30 行, 丢英文 0 行)
    - [中] 14.调用 ngx_signal_process() 处理进程信号，如果有信号，则进入ngx_signal_process方法。例如：例如./nginx -s stop,则处理Nginx的停止信号
    - [中] 6.调用 ngx_save_argv() 保存nginx命令行参数至全局变量ngx_os_argv、ngx_argc、ngx_argv 中；
    - [中] 18.进入进程处理：
    - [中] // 4)初始化模块数组ngx_modules
    - [中] 3.调用ngx_time_init方法，初始化并更新时间，如全局变量ngx_cached_time；
    - [中] 1.调用ngx_get_options方法，主要用于解析命令行中的参数，例如：./nginx -s stop|start|restart
    - [中] 4.调用ngx_log_init方法，初始化日志，并得到日志的文件句柄ngx_log_file.fd；
    - [中] // 5)核心操作，调用ngx_init_cycle创建进程使用的cycle,解析配置文件,启动监听端口
    - [中] 20.多进程工作模式，即 master-worker 多进程工作模式；
    - [中] 9.调用 ngx_crc32_table_init() 初始化CRC表， 初始化一致性hash表，主要作用是加快查询；
    - [中] 7.调用 ngx_process_options() 初始化 init_cycle 的 prefix, conf_prefix, conf_file, conf_param 等字段；
    - [中] 12.调用ngx_preinit_modules方法，主要是前置的初始化模块，初始化每个模块 module 的index，并计算 ngx_max_module；
    - [中] .调用ngx_getpid方法，获取当前进程的pid。一般pid会放在$prefix/nginx.pid的文件中，用于发送重启，关闭等信号命令；
    - [中] // nginx启动的入口函数
    - [中] // 设置重要的指针volatile ngx_cycle_t  *ngx_cycle;
    - [中] // 相关文件ngx_process_cycle.c/ngx_posix_init.c/ngx_process.c
    - [中] // 3)根据命令行参数等建立一个基本的cycle
    - [中] 17.调用 ngx_create_pidfile() 创建进程 ID 记录文件；
    - [中] // 6)启动单进程或多进程
    - [中] 11.通过环境变量 NGINX 完成 socket 的继承，将其保存在全局变量 init_cycle 的 listening 数组中；
    - [中] * Nginx 启动过程如下。
    - [中] 16.若无继承 sockets，则调用 ngx_daemon() 创建守护进程，并设置其标志；
    - [中] 19.单进程工作模式；
    - [中] // 2)初始化操作系统调用接口函数ngx_os_io = ngx_linux_io;
    - [中] // 1)解析命令行参数,显示帮助信息
    - [中] 8.调用 ngx_os_init() 初始化系统相关变量， 如内存页面大小ngx_pagesize,ngx_cacheline_size,最大连接数ngx_max_sockets等；
    - [中] 15.调用 ngx_init_signals() 注册相关信号；
    - [中] 10.调用 ngx_add_inherited_sockets() 继承 socket的套接字。主要作用是热启动的时候需要平滑过渡；
    - [中] 5.创建全局变量 init_cycle 的内存池 pool；
    - [中] 13.调用 ngx_init_cycle() 进行初始化全局变量 init_cycle，这个步骤非常重要；

## src/http/ngx_http_write_filter_module.c  (用户注释 116 行; 丢中文 21 行, 丢英文 7 行)
    - [中] * 因为当响应报文一次性不能发送完成时，会把剩余的响应报文保存在out中，
    - [中] //如果len小于postpone_output,并且没有flush和last,则延迟发送， 直接返回
    - [中] * 相对于本次发送的响应报文数据in来说（即该方法所传入的参数in），
    - [中] /* 重新调整链表缓冲区out的情况，把已发送数据的缓冲区内存回收 */
    - [中] * c->send_chain的取值在不同操作系统，编译选项以及协议下会取不同的函数
    - [中] //chain之前的buf已经消费掉了，可以清理了
    - [中] //https://nginx.org/en/docs/http/ngx_http_core_module.html#postpone_output 默认1460
    - [中] * 并计算待发送响应报文总的长度size；
    - [中] * 遍历当前请求out链表缓冲区，计算剩余响应报文的长度；
    - [中] //开始遍历上一次还没有传输完毕的chain，如果这次没有传完的里面还有的话，就跳出循环，否则free这个chain
    - [中] /* 发送数据，返回值为还没有发送完的chain */
    - [中] * 则本次不能发送响应报文，继续保存在out链表缓冲区中，以待下次才发送；
    - [中] * 若out链表最后一块缓冲区last为空，且没有强制性刷新flush链表缓冲区out，
    - [中] //in 是本次要发送的；out是上次还没发送完的(因为tcp写缓冲区空间有限， 上次可能仅仅发送了部分，剩余的保存在r->out中)
    - [中] //将r->out中的数据输出， ngx_writev_chain / ngx_linux_sendfile_chain / ngx_ssl_send_chain
    - [中] * 将本次待发送的响应报文的缓冲区in添加到out链表缓冲区的尾部，
    - [中] //遍历in链表，将其buf复制in到out链表。用来链接新的chain到out chain后面
    - [中] //遍历r->out, 直到最后一个节点。计算out缓冲区数据大小, 统计flush/sync/last标识。out chain，也就是上次没有发送完成的chain buf
    - [中] * 且当前有待发响应报文in，但是待发送响应报文总的长度size小于预设可发送条件值postpone_output,
    - [中] * out链表缓冲区保存的是前一次剩余的响应报文；
    - [中] * 其中postpone_output预设值我们可以在配置文件nginx.conf中设置；
    - [英] ngx_linux_sendfile_chain,
    - [英] ngx_writev_chain,
    - [英] //the size of all bufs is smaller than "postpone_output" directive
    - [英] *   #if (NGX_HAVE_SENDFILE)
    - [英] #endif
    - [英] NGX_IO_SENDFILE
    - [英] #else

## src/core/ngx_hash.h  (用户注释 147 行; 丢中文 27 行, 丢英文 0 行)
    - [中] * 该字段如果为NULL，那么调用完初始化函数后，该字段指向新创建出来的hash表。
    - [中] *        这个hash表查询不出正确结果。
    - [中] // Nginx散列表是只读的，初始化后不能修改，只能查找
    - [中] //1.一个极其简易的散列表，它以数组的形式保存着 hsize个元素(槽位)，每个元素都是 ngx_array_t动态数组。
    - [中] *        实际上是构造了一个hash表的一个“链表”，是通过hash表中的key“链接”起来的。比如：对于“*.abc.com”将会构造出2个hash表，
    - [中] // 初始化通配符散列表hinit
    - [中] * 该值在调用的过程中用来保存和检测是否有冲突的key值，也就是是否有重复
    - [中] *        直到在某一级的hash表中查到的表项对应的value对应一个真正的值而非一个指向下一级hash表的指针的时候，查询过程结束。
    - [中] * 如果该字段不为NULL，那么在初始完成的时候，所有的数据被插入了这个字段所指的hash表中。
    - [中] // 待初始化的散列表结构体
    - [中] *        或者说是在4的倍数的地址上是对齐的。因为这个value的值的低两位bit是有用的，所以必须为0。如果不满足这个条件，
    - [中] * hint: 构造一个通配符hash表的一些参数的一个集合。关于该参数对应的类型的说明，请参见ngx_hash_t类型中ngx_hash_init函数的说明。
    - [中] // 桶的最大大小，即ngx_hash_elt_t加自定义数据,  它限制了每个散列表元素关键字的最大长度
    - [中] *        例如：“*.abc.com”或者“.abc.com”被预处理完成以后，变成了“com.abc.”。而“mail.xxx.*”则被预处理为“mail.xxx.”。
    - [中] //在用户添加的元素过程中，会根据用户的 ngx_str_t类型的关键字hash值添加到 ngx_array_t动态数组中。
    - [中] * nelts: names数组元素的个数。
    - [中] //这里所有的用户元素的关键字都不可以带通配符，表示精确匹配
    - [中] * 如果在初始化一个hash表的时候，发现某个桶里面无法存的下所有属于该桶的元素，则hash表初始化失败
    - [中] *        为什么会被处理这样？这里不得不简单地描述一下通配符hash表的实现原理。当构造此类型的hash表的时候，
    - [中] * 这是个二维数组，第一个维度代表的是bucket的编号，那么keys_hash[i]中存放的是所有的key算出来的hash值对hsize取模以后的值为i的key。
    - [中] *        通过查com可以找到第二级的hash表，在第二级hash表中，再查找abc，依次类推，
    - [中] * 假设有3个key,分别是key1,key2和key3假设hash值算出来以后对hsize取模的值都是i，那么这三个key的值就顺序存放在keys_hash[i][0],keys_hash[i][1], keys_hash[i][2]。
    - [中] * names: 构造此hash表的所有的通配符key的数组。特别要注意的是这里的key已经都是被预处理过的。
    - [中] *        这里有一点需要特别注意的，就是names数组中元素的value所对应的值（也就是真正的value所在的地址）必须是能被4整除的，
    - [中] *        第一个hash表中有一个key为com的表项，该表项的value包含有指向第二个hash表的指针，而第二个hash表中有一个表项abc，
    - [中] // 函数执行后把names数组里的元素放入散列表，可以hash查找
    - [中] *        该表项的value包含有指向*.abc.com对应的value的指针。那么查询的时候，比如查询www.abc.com的时候，先查com，

## src/http/ngx_http_request_body.c  (用户注释 310 行; 丢中文 27 行, 丢英文 0 行)
    - [中] * 还存在未处理的数据，则证明在接收HTTP请求头部期间，已经预接收了HTTP请求包体；
    - [中] * 具体意义是：客户端期望发送请求包体，服务器允许客户端发送，
    - [中] * 在请求结构ngx_http_request_t 成员中header_in缓冲区保存的是HTTP请求头部，
    - [中] * 若未接接收到完整的HTTP请求包体，且当前连接读事件未准备就绪，
    - [中] * 若在接收HTTP请求头部过程没有预接收HTTP请求包体数据，
    - [中] //不可读或已读完
    - [中] * 同时在接收到完整包体后执行回调方法post_handler；
    - [中] * 若客户端已发送该头部表示期望发送请求包体数据，则服务器回复HTTP/1.1 100 Continue；
    - [中] * 并判断是否需要写入临时文件，以及是否接收全部的请求包体，
    - [中] //处理 Expect请求头
    - [中] * 将TCP套接字连接缓冲区中当前的字符流全部读取出来，
    - [中] * 则需将读事件添加到定时器机制，注册到epoll事件机制中，等待可读事件发生；
    - [中] //读取请求体， 数据被读到r->request_body中
    - [中] /* 若ngx_http_request_body_filter返回NGX_OK，则继续执行以下程序 */
    - [中] //至此说明 没有预读的请求体数据
    - [中] * ngx_http_test_expect 用于检查客户端是否发送Expect:100-continue头部，
    - [中] //更新已经读取到的数据（r->header_in->last - r->header_in->pos 通常为0， 因为在ngx_http_request_body_filter中已经消费过out了）
    - [中] * 或者预接收了不完整的HTTP请求包体，但是header_in缓冲区不够继续存储剩余的包体；
    - [中] //处理preread部分数据
    - [中] * 由于在处理HTTP请求之前，HTTP头部已被完整接收，所以若header_in缓冲区里面
    - [中] //preread是读取请求头时读取到的请求头的长度
    - [中] * 则计算还需接收HTTP请求包体的大小rest；
    - [中] /* 若指定HTTP请求包体的content_length字段大于0，则表示需要接收包体；*/
    - [中] * 真正开始接收请求包体数据；
    - [中] * 进一步计算待需接收HTTP请求的大小rest；
    - [中] * 若header_in缓冲区存在预接收的HTTP请求包体，
    - [中] * 该函数返回NGX_OK；

## src/core/ngx_cycle.h  (用户注释 136 行; 丢中文 11 行, 丢英文 15 行)
    - [中] //共享内存区域列表，每个列表通过调用 ngx_shared_memory_add() 函数进行添加. 共享区域映射到所有nginx进程相同的地址范围，并用于共享常见数据，例如HTTP缓存
    - [中] // free_connections,空闲连接，使用指针串成单向链表
    - [中] // nginx核心数据结构，表示nginx的生命周期，含有许多重要参数
    - [中] // 存储所有模块的配置结构体，是个二维数组  conf_ctx = ngx_pcalloc(pool, ngx_max_module * sizeof(void *));
    - [中] // connections/read_events/write_events,连接池,大小是connection_n
    - [中] //单链表容器，元素的类型是 ngx_shm_zone_t结构体，每个元素表示一块共享内存，存放所有的共享内存
    - [中] //动态数组,每个数组元素都是ngx_listening_t结构体,代表nginx监听的多个端口, 表示监听端口及相关的参数
    - [中] // listening,监听的端口数组
    - [中] // conf_ctx, 存储所有模块的配置结构体，是个二维数组
    - [中] * 无论是master、worker工、cache manager（loader）进程，都有唯一一个全局变量ngx_cycle_t结构体
    - [中] //保存着所有模块存储配置项的结构体的指针，它首先是一个数组，每个数组成员又是一个指针，这个指针指向另一个存储着指针的数组
    - [英] * Shared zones are mapped to the same address range in all nginx processes and are used to share common data, for example the HTTP cache in-memory tree.
    - [英] // 5 = ngx_epoll_module
    - [英] // 8 = ngx_http_core_module
    - [英] // 4 = ngx_event_core_module
    - [英] * https://nginx.org/en/docs/dev/development_guide.html#cycle
    - [英] * Listen sockets are created based on the listening objects.
    - [英] * List of shared memory zones, each added by calling the ngx_shared_memory_add() function.
    - [英] * */
    - [英] * Listening objects are normally added by the listen directive of different modules which call the ngx_create_listening() function.
    - [英] // 7 = ngx_http_module
    - [英] // 3 = ngx_event_module
    - [英] // 0 = ngx_core_module
    - [英] *  Array of core module configurations. The configurations are created and filled during reading of nginx configuration files.
    - [英] * Array of listening objects of type ngx_listening_t.
    - [英] // 1 = ngx_errlog_module

## src/http/ngx_http_upstream.c  (用户注释 645 行; 丢中文 20 行, 丢英文 6 行)
    - [中] //以下处理需要缓存 u->buffering = 1，即上游网速优先的场景
    - [中] //未找到upstream配置块
    - [中] //为当前请求创建per request的负载均衡数据结构体
    - [中] * 初始化负载均衡算法：
    - [中] * 指定r->upstream->peer.tries，请求最多允许尝试这么多个后端
    - [中] * 若不超时，则以固定内存块方式转发响应包体给下游服务器，
    - [中] * 则需要调用ngx_http_upstream_next方法尝试与下一个上游服务器建立连接；
    - [中] * init 在收到请求后，在进行执行负载均衡算法前调用
    - [中] * 指定r->upstream->peer.free，当不用该后端时，进行数据的更新（不管成功或失败都调用）
    - [中] * 若已经指定了上游服务器地址，则不需要解析，
    - [中] //这个方法才是真正决定以固定内存块作为缓存时如何转发响应的，注意，传递的第 2个参数是 0
    - [中] * 直接调用ngx_http_upstream_connection方法向上游服务器发起连接；
    - [中] * 且ngx_http_upstream_test_connect方法返回非NGX_OK，标志当前还未与上游服务器成功建立连接；
    - [中] * 对于 upstream 机制的加权轮询策略来说该方法就是 ngx_http_upstream_init_round_robin_peer
    - [中] * 指定r->upstream->peer.get，用于从集群中选取一台后端服务器;
    - [中] /* 初始化ngx_event_pipe_t结构体 p */
    - [中] * 若标志位request_sent为0，表示还未发送请求；
    - [中] //需要http模块提前创建，否则会报错
    - [中] //还没发送请求且连接上存在错误，则尝试next upstream
    - [中] * 注意：转发的过程中，会接收来自上游服务器的响应包体；
    - [英] so it's a convenient place to keep track of the number of connection failures, or a computed
    - [英] backend server to service that request; this structure is persistent across backend re-tries,
    - [英] It sets up a data structure that the module will use as it tries to find an appropriate
    - [英] * "The peer initialization function is called once per request.
    - [英] hash value. By convention, this struct is called ngx_http_upstream_<module_name>_peer_data_t."
    - [英] **/

## src/event/modules/ngx_epoll_module.c  (用户注释 224 行; 丢中文 23 行, 丢英文 0 行)
    - [中] * 若没有设置timer_resolution配置项时，
    - [中] * 若设置timer_resolution配置项，
    - [中] * 则每隔timer_resolution配置项参数会设置ngx_event_timer_alarm为1，表示需要更新时间；
    - [中] //当fd套接字描述符为 -1或者 instance标志位不相等时，表示这个事件已经过期了，不用处理
    - [中] * 同一连接的读写事件的instance标志位是相同的；
    - [中] // ready表示还未处理的事件。当 ready大于0时继续处理
    - [中] /* 获取事件关联的连接 */
    - [中] * NGX_UPDATE_TIME 标志表示每次调用epoll_wait函数返回后需要更新时间；
    - [中] * 若一个事件所关联的连接已经在epoll对象的监控中，则只需修改事件的类型即可；
    - [中] //Nginx对时间的缓存和管理。当 flags标志位指示要更新时间时，就是在这里更新的
    - [中] //无论是 32位还是 64位机器，其地址的最后 1位肯定是 0，可以用下面这行语句把ngx_connection_t的地址还原到真正的地址值
    - [中] // 在开启了负载均衡锁且获取到了锁后，flags参数中会含有 NGX_POST_EVENTS，表示这批事件要延后处理
    - [中] * 这里在判断事件类型是可读还是可写，必须根据事件的active标志位来判断事件是否活跃；
    - [中] //设置用于异步 I/O完成通知的 ngx_eventfd_event事件，它与 ngx_eventfd_conn连接是对应的
    - [中] * 因为epoll_ctl函数有添加add和修改mod模式，
    - [中] //下面会根据 event参数确定当前事件是读事件还是写事件，这会决定 events是加上 EPOLLIN标志位还是EPOLLOUT标志位
    - [中] /* 屏蔽连接对象的最低位，即获取连接对象的真正地址 */
    - [中] * 若fd描述符为-1，或连接对象读事件的instance标志位不相同，则判为过期事件；
    - [中] * 这样做的情况是避免与事件相关联的连接两次注册到epoll对象中；
    - [中] /* 设置异步IO事件ngx_eventfd_event，该事件是ngx_eventfd对应的ngx_event事件 */
    - [中] /* 遍历ready，处理异步IO事件 */
    - [中] * 若一个事件所关联的连接没有在epoll对象的监控中，则需要将其相应的事件类型注册到epoll对象中；
    - [中] //每个事件的 data成员都存放着其对应的 ngx_connection_t连接

## src/http/ngx_http_core_module.c  (用户注释 654 行; 丢中文 22 行, 丢英文 1 行)
    - [中] //之前，在ngx_http_handler函数中它被设置为ngx_http_core_run_phases。
    - [中] //内部请求，从server_rewrite阶段开始执行
    - [中] //如果是HTTP/1.1版本，则保持连接
    - [中] /* 获取ngx_http_core_module模块的main级别的配置项结构 */
    - [中] * 先把 write_event_handler强制转化为ngx_http_request_empty_handler，
    - [中] * of 为打开文件信息
    - [中] /* 没有handler的情况。 */
    - [中] //NGX_OK:精确匹配; NGX_DONE: 需客户端重定向，不再继续往下执行
    - [中] * 设置of的标志位disable_symlinks，是否禁用符号连接
    - [中] //从头开始重新执行
    - [中] * ngx_http_phase_handler_t *handlers数组中的第一个回调方法；
    - [中] * phase_handler序号设置为0，表示执行ngx_http_phase_engine_t结构体成员
    - [中] * 可以防止该HTTP模块异步地 处理请求时却有其他HTTP模块还在同时处理可写事件、向客户端发送响应
    - [中] //为不做任何事的空方法
    - [中] * HTTP 框架在这一阶段调用HTTP模块处理请求就意味着接下来只希望该模块处理请求，
    - [中] //如果设置了connection_type为keep-alive
    - [中] //如果uri以/结尾
    - [中] //把clcf添加到父级别(server级别、location级别)clcf的locations双链表中。
    - [中] //如果设置了connection_type为close
    - [中] //未设置connection_type
    - [中] /* 将ngx_http_location_queue_t添加到双向链表中 */
    - [中] * path 为文件路径
    - [英] //https://nginx.org/en/docs/http/ngx_http_core_module.html#disable_symlinks

## src/event/ngx_event_connect.h  (用户注释 47 行; 丢中文 12 行, 丢英文 9 行)
    - [中] *  NGX_PEER_KEEPALIVE — Currently unused
    - [中] NGX_BUSY — no servers are currently available. This can happen due to many reasons, including: the dynamic server group is empty,
    - [中] /* 为每个请求专用的负载均衡数据，一般指向ngx_http_upstream_<name>_peer_data_t */
    - [中] *  NGX_PEER_FAILED — Attempt was unsuccessful
    - [中] //这个 data指针仅用于和上面的 get、 free方法配合传递参数，它的具体含义与实现 get方法、 free方法的模块相关，
    - [中] // 与 get方法对应的释放连接的方法
    - [中] * NGX_OK — Server was selected.
    - [中] *  NGX_PEER_NEXT — A special case when upstream server returns codes 403 or 404, which are not considered a failure.
    - [中] //可参照 ngx_event_get_peer_pt和 ngx_event_free_peer_pt方法原型中的 data参数
    - [中] //获取连接的方法，如果使用长连接构成的连接池，那么必须要实现 get方法
    - [中] NGX_DONE — the underlying connection was reused and there is no need to create a new connection to the upstream server.
    - [中] NGX_ERROR — Internal error occurred.
    - [英] all servers in the group are in the failed state, or all servers in the group are already handling the maximum number of connections.
    - [英] This value is set by the keepalive module.
    - [英] * The return is one of:
    - [英] //The method has to fill the sockaddr, socklen, and name fields of ngx_peer_connection_t structure
    - [英] //The method called when the upstream module is ready to pass a request to an upstream server and needs to know its address
    - [英] //free(pc, data, state)
    - [英] //The state argument is the completion status of the upstream connection, a bitmask with the following possible values:
    - [英] //The method called when an upstream module has finished work with a particular server.
    - [英] //get(pc, data)

## src/core/ngx_hash.c  (用户注释 220 行; 丢中文 18 行, 丢英文 0 行)
    - [中] * 这样来估计hash表所需的最小bucket数量
    - [中] * 那么hash表的bucket所需的数量就越少，但至少需要一个bucket
    - [中] * 如果超过了bucket_size，则说明需要调整
    - [中] * 检查是否频繁的标准是：判断元素总长度和bucket桶的容量bucket_size做比较
    - [中] /* 估计hash表最少bucket数量；
    - [中] * 重新初始化test数组元素，初始值为一个指针大小
    - [中] * test是用来做探测用的，探测的目标是在当前bucket的数量下，冲突发生的是否频繁。
    - [中] //test数组初始化为每个元素都等于一个指针大小
    - [中] * 探测会遍历所有的元素，并且计算落到同一个bucket上元素长度的总和和bucket_size比较
    - [中] // start 是最少需要的桶的数量
    - [中] //逐步调整，找到一个能放下所有元素的桶数量。
    - [中] //初始化test为 存放max_size个u_short的数组。 每个元素存放的是i对应槽位上所有key元素的长度。
    - [中] * 每个关键字元素需要的内存空间是 NGX_HASH_ELT_SIZE(&name[n])，至少需要占用两个指针的大小即2*sizeof(void *)
    - [中] * 这边就是真正的探测逻辑
    - [中] * 过于频繁则需要调整桶的个数。
    - [中] * 最终会探测出比较合适的桶的个数 ：size
    - [中] * 因为关键字元素内存越小，则每个bucket所容纳的关键字元素就越多
    - [中] /* 到此已经找到合适的bucket数量，即为size

## src/http/modules/ngx_http_gzip_filter_module.c  (用户注释 161 行; 丢中文 9 行, 丢英文 8 行)
    - [中] uLong               total_in;                // 将要压缩数据缓冲区的长度
    - [中] z_const char        *msg;                    // 存放最近的错误信息，NULL表示没有错误
    - [中] uLong               total_out;               // 压缩后数据缓冲区的大小
    - [中] Bytef               *next_out;               // 压缩后数据保存位置。
    - [中] uInt                 avail_in;               // 将要压缩数据的长度
    - [中] z_const Bytef       *next_in;                // 将要压缩数据的首地址
    - [中] //gzip实现结构体
    - [中] uInt                 avail_out;              // 压缩后数据的长度
    - [中] int               data_type;                // 表示数据类型，文本或者二进制
    - [英] free_func         zfree;
    - [英] voidpf            opaque;
    - [英] * typedef struct z_stream_s {
    - [英] }  z_stream;
    - [英] uLong             adler;
    - [英] uLong             reserved;
    - [英] alloc_func        zalloc;
    - [英] struct internal_state FAR *state;

## src/core/ngx_palloc.c  (用户注释 126 行; 丢中文 15 行, 丢英文 0 行)
    - [中] * 创建一个ngx_pool_t,  size指定了预分配的内存大小
    - [中] // 内存池的大小可以超过4k
    - [中] * 分配过程一般为，首先判断待分配的内存是否大于 pool->max，如果大于则使用 ngx_palloc_large 在 large 链表里分配一段内存并返回，
    - [中] // 字节对齐分配一个size - sizeof(ngx_pool_t)80字节内存
    - [中] // 多用于创建结构体
    - [中] * 如果小于测尝试从链表的 pool->current 开始遍历链表，尝试找出一个可以分配的内存，当链表里的任何一个节点都无法分配内存的时候，
    - [中] * p->large 存放大内存块列表
    - [中] // 分配对齐的内存，速度快，可能有少量浪费
    - [中] // 设置可用的内存，减去了自身的大小80字节
    - [中] * Nginx会分配一块大内存，其中内存头部存放ngx_pool_t本身内存池的数据结构
    - [中] * 就调用 ngx_palloc_block 生成链表里一个新的节点， 并在新的节点里分配内存并返回， 同时， 还会将pool->current 指针指向新的位置（从链表里面pool->d.failed小于等于4的节点里找出）
    - [中] * 从进程的堆中预分配更多的内存（ngx_create_pool的size参数决 定预分配大小），
    - [中] * p->cleanup 存放可以被回调函数清理的内存块（该内存块不一定会在内存池上面分配）
    - [中] * ngx_pool_data_t	p->d 存放内存池的数据部分（适合小于p->max的内存块存储）
    - [中] * 而后直接使用这块内存的一部分作为小块内存返回给申请者，以此实现减少碎片和调用malloc的次数。

## src/os/unix/ngx_process.c  (用户注释 94 行; 丢中文 14 行, 丢英文 1 行)
    - [中] * 通常，在父、子进程通信前，会先调用socketpair方法创建这样一组套接字，在调用fork方法创建出子进程后，
    - [中] * 当socketpair执行成功时，sv[2]这两个套接字具备下列关系：向sv[0]套接字写入数据， 将可以从sv[1]套接字中读取到刚写入的数据；
    - [中] * 返回0表示成功，-1表示失败
    - [中] /* 如果pid fork成功，则调用 ngx_worker_process_cycle方法 */
    - [中] //ngx_processes 定义了Nginx服务中所有的进程，包括master进程和 worker进程
    - [中] * type取值为SOCK_STREAM或者SOCK_DGRAM，它表示在 套接字上使用的是TCP还是UDP
    - [中] * 创建一对关联的套接字sv[2]
    - [中] * 同样，向sv[1]套接字写入数据，也可以从 sv[0]中读取到写入的数据
    - [中] * d表示域， 在Linux下通常取值为AF_UNIX
    - [中] * sv[2]是一个含有两个元素的整型数 组，实际上就是两个套接字
    - [中] //ngx_processes[s].channel 数组正是将要用于父、子进程间通信的套接字对
    - [中] * protocol必须传递0
    - [中] * 而在子进程中则关闭 sv[0]套接字，仅使用sv[1]套接字既可以接收父进程发来的数据，也可以向父进程发送数 据
    - [中] * 将会在父进程中关闭sv[1]套接字，仅使用 sv[0]套接字用于向子进程发送数据以及接收子进程发送来的数据；
    - [英] * int socketpair(int d, int type, int protocol, int sv[2]);

## src/http/ngx_http_config.h  (用户注释 54 行; 丢中文 5 行, 丢英文 9 行)
    - [中] *   Location configuration — Applies to a single location, "if" or "limit_except" block. Functions as location-specific settings for a module.
    - [中] //利用结构体变量ngx_http_request_t r获取HTTP模块main、srv、loc级别的配置项结构体 (在runtime阶段使用)
    - [中] //利用结构体变量ngx_conf_t cf获取HTTP模块的main、srv、loc级别的配置项结构体 (在configuration阶段使用)
    - [中] *   Main configuration — Applies to the entire http block. Functions as global settings for a module.
    - [中] *   Server configuration — Applies to a single server block. Functions as server-specific settings for a module.
    - [英] * The following macros are available for accessing configuration for HTTP modules at runtime.
    - [英] * Each HTTP module can have three types of configuration:
    - [英] * which allocate the structures, initialize them and merge them.
    - [英] * The main configuration of a request never changes.
    - [英] * They all take ngx_conf_t reference as the first argument.
    - [英] * The following macros are available. for accessing configuration for HTTP modules at configuration time.
    - [英] * Configuration structures are created at the nginx configuration stage by calling functions,
    - [英] * Server configuration can change from the default after the virtual server for the request is chosen.
    - [英] * Location configuration selected for processing a request can change multiple times as a result of a rewrite operation or internal redirect

## src/core/ngx_module.h  (用户注释 60 行; 丢中文 8 行, 丢英文 5 行)
    - [中] * HTTP类型的模块主要处理 HTTP应用层的功能。这样，每个模块都有了自己的特性，而 ctx将会指向特定类型模块的公共接口。
    - [中] * core模块的ctx
    - [中] * ctx用于指向一类模块的上下文结构体，为什么需要ctx呢？因为前面说过，
    - [中] //每种模块都有具体的ctx， 如ngx_http_module_t、ngx_event_module_t。对于NGX_CORE_MODULE 为 ngx_core_module_t
    - [中] //master进程启动时回调, 但是目前并没有使用
    - [中] * 例如，在HTTP模块中， ctx需要指向ngx_http_module_t结构体
    - [中] * 在 Nginx的启动、停止过程中，以下 7个函数指针表示有 7个执行点会分别调用这7种方法
    - [中] * Nginx模块有许多种类，不同类模块之间的功能差别很大。例如，事件类型的模块主要处理 I/O事件相关的功能，
    - [英] *      ngx_str_t             name;
    - [英] *   } ngx_core_module_t;
    - [英] *      char               *(*init_conf)(ngx_cycle_t *cycle, void *conf);
    - [英] *   typedef struct {
    - [英] *      void               *(*create_conf)(ngx_cycle_t *cycle);

## src/core/ngx_shmtx.c  (用户注释 45 行; 丢中文 11 行, 丢英文 2 行)
    - [中] * 通过文件锁实现, 即通过对fcntl系统调用封装过的ngx_trylock_fd、ngx_lock_fd和ngx_unlock_fd方法实现的锁
    - [中] * 直到其他进程 将信号量sem的值改变为正数后，这时才能继续通过将sem减1而使得当前进程继续向下执行。
    - [中] //初始化ngx_shmtx_t互斥锁
    - [中] // 信号量 mtx->sem初始化为0，用于进程间通信
    - [中] * 因此，sem_post方法可以实现解锁的功能，而sem_wait方法可以实现加锁的功能
    - [中] * 定义一个sem_t类型的变量后，即可围绕着它使用信号量。使用前，先要调用 sem_init方法初始化信号量
    - [中] * pshared将指明sem信号量是用于进程间 同步还是用于线程间同步, 当pshared为0时表示线程间同步，而pshared为1时表示进程间同 步
    - [中] * sem即为定义的信号量
    - [中] * 调用sem_wait方法将会把信号量sem的值减1， 如果sem值已经小于或等于0了，则阻塞住当前进程（进程会进入睡眠状态），
    - [中] * 最初的信号量sem值为0，调用sem_post方法 将会把sem值加1，这个操作不会有任何阻塞；
    - [中] * 数value表示信号 量sem的初始值
    - [英] * int sem_init(sem_t *sem, int pshared, unsigned int value);
    - [英] * */

## src/event/ngx_event_pipe.c  (用户注释 142 行; 丢中文 12 行, 丢英文 0 行)
    - [中] //如果preread_bufs中有内容，应该优先处理这部分数据，而不是接收更多包体
    - [中] * 即该数据是接收响应头部区间，预接收的响应包体；
    - [中] //upstream_blocked为1表示执行后需要暂时停止读取上游响应，需要通过向下游发送
    - [中] //调用recv_chain方法接收上游的响应
    - [中] //1. 首先检查preread_bufs缓冲区，这个缓冲区存放这接收响应包头时可能接收到的包体
    - [中] * 若有缓冲区接收上游响应包体，则调用recv_chain方法接收上游响应包体；
    - [中] //响应来清理出空闲缓冲区，以供ngx_event_pipe_read_upstream方法再次读取上游响应
    - [中] * 则没有可读的响应数据，break退出for循环；
    - [中] * 若标志位read和upstream_blocked为0，
    - [中] * 若preread_bufs链表缓冲区有未处理的数据，需要把它挂载到chain链表中
    - [中] //read标志位为1表示ngx_event_pipe_read_upstream方法执行后读取到了数据
    - [中] * 把接收到的上游响应包体缓冲区添加到free_raw_bufs链表的尾端；

## src/core/ngx_log.h  (用户注释 93 行; 丢中文 8 行, 丢英文 3 行)
    - [中] * file — Logging to a file
    - [中] * 支持以下类型的输出：
    - [中] * memory — Logging to internal memory storage for development purposes; the memory can be accessed later with a debugger
    - [中] * stderr — Logging to standard error (stderr)
    - [中] // 因为使用了APPEND，所以多进程写文件是安全的
    - [中] // 一个日志链表可以理解为其他日志模型里的category
    - [中] * syslog — Logging to syslog
    - [中] // 错误日志结构体
    - [英] // cycle->log, cscf->error_log, clcf->error_log
    - [英] * https://nginx.org/en/docs/dev/development_guide.html#logging
    - [英] * A logger instance can be a chain of loggers, linked to each other with the next field. In this case, each message is written to all loggers in the chain.

## src/http/ngx_http_upstream_round_robin.c  (用户注释 208 行; 丢中文 10 行, 丢英文 1 行)
    - [中] * 2.从集群中选出current_weight最大的peer，作为本次选定的后端。
    - [中] * 3.对于本次选定的后端，执行：peer->current_weight -= total。
    - [中] * current_weight: 后端目前的权重，一开始为0，之后会动态调整
    - [中] * 加权轮询算法可描述为：
    - [中] * 1.对于每个请求，遍历集群中的所有可用后端，对于每个后端peer执行：
    - [中] * weight: 配置文件中指定的该后端的权重，这个值是固定不变的。
    - [中] *    同时累加所有peer的effective_weight，保存为total。
    - [中] //https://www.taohui.pub/2021/02/08/nginx/深入剖析Nginx负载均衡算法/
    - [中] * effective_weight: 后端的有效权重，初始值为weight。
    - [中] *    peer->current_weight += peer->effecitve_weight。
    - [英] //https://www.kancloud.cn/digest/sknginx/130030

## src/os/unix/ngx_files.c  (用户注释 62 行; 丢中文 10 行, 丢英文 1 行)
    - [中] *  使用F_SETLKW时则不同，锁被占用后fcntl方法会一直等待，在其他进程没有释放锁时，当前进程就会阻塞在fcntl方法中，
    - [中] * lock描述了这个锁的信息
    - [中] * 基于文件的互斥锁
    - [中] //  获取fd对应的互斥锁，如果返回 -1，则这时的 ngx_errno将保存错误码
    - [中] *  使用F_SETLK时如果互斥锁已经被其他进程占用，fcntl方法不会等待其他进程释放锁且自己拿到锁后才返回，而是立即返回获取互斥锁失败；
    - [中] * cmd参数在Nginx中只会有两个值：F_SETLK和F_SETLKW，它们都表示试图获得 互斥锁，
    - [中] *          这种阻塞会导致当前进程由可执行状态转为睡眠状态
    - [中] * cmd表示执行的锁操作
    - [中] // F_SETLK意味着不会导致进程睡眠
    - [中] * fd是打开的文件句柄
    - [英] * int fcntl(int fd, int cmd, struct flock *lock);

## src/core/ngx_conf_file.h  (用户注释 61 行; 丢中文 7 行, 丢英文 3 行)
    - [中] *   NGX_HTTP_LOC_CONF_OFFSET：使用 create_loc_conf 方法产生的结构体来存储解析出的配置项参数；
    - [中] //在配置文件中的偏移量, NGX_HTTP_(MAIN|SRV|LOC)_CONF_OFFSET
    - [中] *   NGX_HTTP_MAIN_CONF_OFFSET：使用create_main_conf 方法产生的结构体来存储解析出的配置项参数；
    - [中] // 配置项读取后的处理方法，必须是 ngx_conf_post_t结构的指针
    - [中] *   NGX_HTTP_SRV_CONF_OFFSET：使用 create_srv_conf 方法产生的结构体来存储解析出的配置项参数；
    - [中] //支持的回调方法；大多数情况为NULL
    - [中] * 用于指示配置项所处内存的相对偏移量，仅在type 中没有设置NGX_DIRECT_CONF 和NGX_MAIN_CONF 时才生效。对于HTTP 模块，conf 必须设置，它的取值如下：
    - [英] * has two purposes:
    - [英] *   2. to pass additional data to the main handler. In the first case, the ngx_conf_post_t structure needs to be initialized with a pointer to the handler
    - [英] *   1. be used to define a handler to be called after the main handler has completed

## src/core/ngx_cycle.c  (用户注释 151 行; 丢中文 10 行, 丢英文 0 行)
    - [中] //遍历old_cycle，统计上一次系统中分配了多少块共享内存，接着就按这个数据初始化当前cycle中共享内存的规模
    - [中] * 新的共享内存与旧的共享内存链表进行比较，
    - [中] //并调用该模块在ngx_command_t 结构体中定义的配置项处理方法
    - [中] * 相同则保留，不同创建新的，旧的被释放
    - [中] //nginx根据以往的经验（old_cycle）预测这一次的配置需要分配多少内存
    - [中] //对于任一个配置项，将会检查所有核心模块以找出对它感兴趣的模块，
    - [中] /* 解析配置文件/usr/local/nginx/conf/nginx.conf 信息 */
    - [中] //遍历old_cycle的shared_memory链表
    - [中] /* 初始化shared_memory链表 */
    - [中] //调用配置模块提供的解析配置项方法。

## src/core/ngx_connection.h  (用户注释 126 行; 丢中文 6 行, 丢英文 3 行)
    - [中] //recv, send, recv_chain, send_chain — I/O operations for the connection.
    - [中] // 对于http为 ngx_send; 对于https为 ngx_ssl_write
    - [中] /* 发送网络字符流的方法，是一个函数指针，指向发送函数 */
    - [中] // 对于http为 ngx_recv; 对于https为 ngx_ssl_recv
    - [中] // 链接的 SSL 信息. SSL context for the connection.
    - [中] /* 接收网络字符流的方法，是一个函数指针，指向接收函数 */
    - [英] * In this case the connection's ssl field holds a pointer to an ngx_ssl_connection_t structure, keeping all SSL-related data for the connection, including SSL_CTX and SSL.
    - [英] * The recv, send, recv_chain, and send_chain handlers are set to SSL-enabled functions as well.
    - [英] * An nginx connection can transparently encapsulate the SSL layer.

## src/os/unix/ngx_shmem.c  (用户注释 17 行; 丢中文 9 行, 丢英文 0 行)
    - [中] * mmap可以将磁盘文件映射到内存中，直接操作内存时Linux内核将负责同步内存和磁盘文件中的数据，
    - [中] * length参数就是将要在 内存中开辟的线性地址空间大小，
    - [中] * offset则代表从文件的这个偏移量处开始共享，当然Nginx没有使用这一特性。
    - [中] *      此时的mmap方法和ngx_shm_alloc的功能几乎完全相同。
    - [中] // 开辟一块 shm->size大小且可以读 /写的共享内存，内存首地址存放在 addr中
    - [中] * start参数说明希望的共享内存起始映射地址，当然，通常都会把start设为NULL空 指针。
    - [中] * prot参数则是操作这段共享内存的方式（如只读或者可读可写），
    - [中] *      当flags参数中加入MAP_ANON或者 MAP_ANONYMOUS参数时表示不使用文件映射方式，这时fd和offset参数就没有意义，也不需要传递了，
    - [中] * fd参数就指向需要同步的磁盘文件，

## src/event/ngx_event_posted.c  (用户注释 18 行; 丢中文 8 行, 丢英文 0 行)
    - [中] * 将epoll_wait产生的一批事件，分到这两个队列中，让存放着新连接事件的 ngx_posted_accept_events队列优先执行，存放普通事件的ngx_posted_events队列最后执行，这
    - [中] * 即新连接事件全部放到ngx_posted_accept_events队 列中，普通事件则放到ngx_posted_events队列中。
    - [中] * 接着再处理ngx_posted_events队列中的事件，这样就大大减少了ngx_accept_mutex锁占 用的时间
    - [中] * 这样，接下来会先处理 ngx_posted_accept_events队列中的事件，处理完后就要立刻释放ngx_accept_mutex锁，
    - [中] 如果在处理一个事件的过程中产生了另一个事件，而我们希望这个事件随后执行（不是立刻执行），就可以把它放到ngx_posted_events队列中
    - [中] * ngx_posted_accept_events队列和 ngx_posted_events队列把这批事件归类了，
    - [中] //由被触发的监听连接的读 事件构成的ngx_posted_accept_events队列
    - [中] 是解决“惊群”和负载均衡两个问题的关键

## src/http/modules/ngx_http_proxy_module.c  (用户注释 355 行; 丢中文 8 行, 丢英文 0 行)
    - [中] /* proxy_cookie_domain domain replacement; 格式  */
    - [中] /* redirect 与响应头的value匹配，需要执行重写逻辑 */
    - [中] //1.将由proxy_pass设置的headers_source复制进headers_merged中
    - [中] /*合并conf->headers_source和default_headers*/
    - [中] //url->data 必须以 http:// 或 https:// 开头
    - [中] //2.如果proxy-pass后面的参数不是变量，则会在配置解析阶段解析后面upstream主机的ip地址并且生成upstream结构并且和ngx_http_proxy_loc_conf_t中的upstream结构连接起来
    - [中] /**此处说明url中不包含变量 */
    - [中] // 添加一组请求头的长度 key: value

## src/http/ngx_http_postpone_filter_module.c  (用户注释 95 行; 丢中文 8 行, 丢英文 0 行)
    - [中] // 调用下一个 HTTP过滤模块转发 out链表中保存的待转发的包体
    - [中] //至此，说明postponed链表中是有子请求产生的响应需要转发的，可以先把in包体加到待转发响应的末尾
    - [中] //r->postponed是一个链表，链表每个节点缓存了一个子请求的响应
    - [中] 则直接发送当前产生的数据in或者继续发送out chain中之前没有发送完成的数据 */
    - [中] // 如果postponed为空，表示请求r没有子请求产生的响应需要转发
    - [中] /* 如果该节点保存的是数据，可以直接处理该节点，将它发送到out chain */
    - [中] /* 当前请求的postponed链表中之前就存在需要处理的节点，则新建一个节点，保存当前产生的数据in，并将它插入到postponed队尾 */
    - [中] /* 到这里，表示当前请求可以往out chain发送数据，如果它的postponed链表中没有子请求，也没有数据，

## src/os/unix/ngx_shmem.h  (用户注释 15 行; 丢中文 7 行, 丢英文 1 行)
    - [中] * 在使用共享内存时，Nginx一般是由master进程创建，在master进程fork出worker子进程后，所有的进程开始使用这块内存中的数据
    - [中] * 有3种实现（不映射文件使用mmap分配共享内存、以/dev/zero文 件使用mmap映射共享内存、用shmget调用来分配共享内存）
    - [中] * 在任何一个进程修改了共享内存中的内容后，其他进程通过访问这段共享内存都能够得到修改后的内容.
    - [中] //用于分配新的共享内存
    - [中] * 共享内存是Linux下提供的最基本的进程间通信方法，它通过mmap或者shmget系统调用在内存中创建了一块连续的线性地址空间，
    - [中] //此结构体 描述共享内存的结构体
    - [中] * 而通过munmap或者shmdt系统调用可以释放这块内存。使用共享内存的好处是当多个进程使用同一块共享内存时，
    - [英] /**

## src/core/ngx_conf_file.c  (用户注释 165 行; 丢中文 7 行, 丢英文 0 行)
    - [中] * cmd为命令结构；
    - [中] * conf为配置的指针地址;
    - [中] * 配置文件设置值；
    - [中] /* 若自定义指令处理函数handler为NULL，则调用Nginx内建的指令解析机制 */
    - [中] //nginx_conf_file.c文件中定义了多种配置文件值设值的方法 如ngx_conf_set_flag_slot/ngx_conf_set_str_slot
    - [中] * conf为配置指针地址 一般情况下 conf为模块自定义的配置文件数据结构地址
    - [中] //遍历所有模块，查找该指令的配置解析函数

## src/core/ngx_slab.h  (用户注释 82 行; 丢中文 5 行, 丢英文 2 行)
    - [中] // 在slots之后
    - [中] * 每个数组元素管理一组相同大小的slab页:
    - [中] /**slots: 在本结构体里并没有相应字段，而是直接把结构体之后的一小块内存解释为slab_slots数组
    - [中] // 目前供商业模块ngx_api来调用，暂无公开接口使用，只能自己定位获取信息
    - [中] // 统计信息数组
    - [英] *   (ngx_slab_page_t *) ((u_char *) (pool) + sizeof(ngx_slab_pool_t))
    - [英] * #define ngx_slab_slots(pool)                                                  \

## src/event/ngx_event_accept.c  (用户注释 81 行; 丢中文 7 行, 丢英文 0 行)
    - [中] * 注意：若是epoll事件机制，这里是不会执行，
    - [中] * 因为epoll事件机制会在调用新连接处理函数ls->handler(c)
    - [中] //将这个新连接对应的读事件添加到epoll等事件驱动模块中，这样，在这个连接上如果接收到用户请求epoll_wait，就会收集到这个事件
    - [中] /* 连接建立错误时的相应处理 */
    - [中] *（实际调用ngx_http_init_connection）时，才会把新连接对应的读事件注册到epoll事件机制中；
    - [中] /* 将新连接对应的读事件注册到事件监控机制中；
    - [中] //如果没有准备好的新连接事件， ngx_event_accept方法会直接返回

## src/http/modules/ngx_http_rewrite_module.c  (用户注释 159 行; 丢中文 7 行, 丢英文 0 行)
    - [中] // 保存着所属location下的所有编译后的脚本（按照顺序）
    - [中] * 在codes数组中分配N个元素存储这1个指令，再依次把指令结构体的内容都拷贝到这N个数组成员中
    - [中] *  该方法必须主动地告知所属指令结构体占用的内存数N，这样从当前指令所在的codes数组索引中加上N后就是下一条指令
    - [中] //一个字节数组，多个字节组成一条指令。存放的是一组指令列表
    - [中] * 2.每次要将1个指令放入codes数组中时，将根据指令结构体的占用内存字节数N，
    - [中] * 1.codes数组设计成每个元素仅占1个字节的大小
    - [中] * 3.HTTP请求到来、脚本指令执行时，每执行完一个脚本指令的ngx_http_script_code_pt方法后，

## src/http/modules/ngx_http_secure_link_module.c  (用户注释 77 行; 丢中文 6 行, 丢英文 1 行)
    - [中] //比较md5值是否一致
    - [中] //$secure_link: "" 表示验签失败，"0" 表示url已经过期
    - [中] //计算出来的hash和url里的start开始的hash进行比较
    - [中] // secure_link_md5 模式
    - [中] //16进制
    - [中] * 本模块提供的连个变量
    - [英] * secure_link $arg_md5,$arg_expires;

## src/core/ngx_rbtree.h  (用户注释 55 行; 丢中文 6 行, 丢英文 0 行)
    - [中] // 表示红黑树添加元素的函数指针，它决定在添加新节点时的行为究竟是替换还是新增
    - [中] * 这时它们在红黑树中的关键字是相同的，然而它们又是不同的节点，这样在添加时就不可以覆盖原有同名关键字节点，
    - [中] * 参考 ngx_rbtree_insert_pt
    - [中] * 处理的主要问题就是当key关键字相同时，继续以何种数据结构作为标准来确定红黑树节点的唯一性
    - [中] * 而是作为新插入的节点存 在。因此，在添加元素时，需要考虑到这种情况。将添加元素的方法抽象出
    - [中] * 很多场合下是允许不同的节点拥有相同的关键字的.例如，不同的字符串可能会散列出相同的关键字，

## src/http/ngx_http_header_filter_module.c  (用户注释 88 行; 丢中文 6 行, 丢英文 0 行)
    - [中] * 将待发送的响应头部挂载到out链表缓冲区中，
    - [中] * 再次调用HTTP框架将out链表中剩余的响应头部字符流继续发送；
    - [中] * 挂载的目的是：当响应头部不能一次性发送完成时，
    - [中] * ngx_http_header_filter方法返回NGX_AGAIN，表示响应头部没有能一次全部发出，
    - [中] //out存放着发往客户端的响应
    - [中] * 则把剩余的响应头部保存在out链表中，以便调用ngx_http_finalize_request时，

## src/core/ngx_array.c  (用户注释 40 行; 丢中文 5 行, 丢英文 0 行)
    - [中] * 则需要重新分配一个新的内存块存储数组，并且将原数组拷贝到新的地址上
    - [中] * 1.如果数组元素的末尾和内存池pool的可用开始的地址相同，并且内存池剩余的空间支持数组扩容，则在当前内存池上扩容
    - [中] * 扩容有两种方式
    - [中] //数组已经满了，需要扩容。
    - [中] * 2. 如果扩容的大小超出了当前内存池剩余的容量或者数组元素的末尾和内存池pool的可用开始的地址不相同，

## src/core/ngx_buf.h  (用户注释 87 行; 丢中文 3 行, 丢英文 2 行)
    - [中] * 标志位，对于操作这块缓冲区时是否使用同步方式，需谨慎考虑，这可能会阻塞Nginx进程，
    - [中] * 有些框架代码在 sync为 1时可能会有阻塞的方式进行 I/O操作，它的意义视使用它的 Nginx模块而定
    - [中] * Nginx中所有操作几乎都是异步的，这是它支持高并发的关键。
    - [英] //By default nginx considers such buffers an error condition, but this flag tells nginx to skip the error check.
    - [英] //Flag indicating that the buffer carries no data or special signal like flush or last_buf.

## src/core/ngx_times.c  (用户注释 36 行; 丢中文 5 行, 丢英文 0 行)
    - [中] * 对于worker进程而言，除了Nginx启动时更新一次时间外，任何更新时间的操作都只能由ngx_epoll_process_events方法执行
    - [中] * 或者ngx_event_timer_alarm标志位为1时，就会调用ngx_time_update方法更新缓存时间
    - [中] * ngx_epoll_process_events当flags参数中有NGX_UPDATE_TIME标志位，
    - [中] * 以下全局变量，缓存当前时间
    - [中] // 格林威治时间 1970年 1月1日凌晨 0点0分0秒到当前时间的毫秒数

## src/event/ngx_event.c  (用户注释 292 行; 丢中文 5 行, 丢英文 0 行)
    - [中] //同时对监听端口的读事件设置处理方法为ngx_event_accept，也就是说，有新连接事件时将调用ngx_event_accept方法建立新连接
    - [中] //在刚刚建立好的连接池中，为所有ngx_listening_t监听对象中的connection成员分配连接，
    - [中] /* 为每个监听套接字分配一个连接结构 */
    - [中] /* 否则(未使用accept锁)，将该监听句柄直接挂载上事件处理模型 */
    - [中] //将监听对象连接的读事件添加到事件驱动模块中，这样，epoll等事件模块就开始检测监听服务，并开始向用户提供服务了

## src/http/modules/ngx_http_referer_module.c  (用户注释 79 行; 丢中文 4 行, 丢英文 1 行)
    - [中] //模块配置结构体
    - [中] * $invalid_referer  Empty string, if the “Referer” request header field value is considered valid, otherwise “1”
    - [中] * 通过Referer请求头拦截请求
    - [中] * 本模块只是提供了一个变量，$invalid_referer。 为1时，表示referer不合法
    - [英] * https://nginx.org/en/docs/http/ngx_http_referer_module.html

## src/http/ngx_http_upstream_round_robin.h  (用户注释 47 行; 丢中文 1 行, 丢英文 4 行)
    - [中] //配合fail_timeout使用，记录上次检查时间。用于检查是否超过了"一段时间"
    - [英] *          best->checked = now;
    - [英] //if (now - peer->checked <= peer->fail_timeout)
    - [英] *      }
    - [英] *     if (now - best->checked > best->fail_timeout) {

## src/core/ngx_proxy_protocol.c  (用户注释 50 行; 丢中文 3 行, 丢英文 0 行)
    - [中] // v2 固定的signature
    - [中] // 代理协议必须以PROXY开始
    - [中] /** v1协议解析 */

## src/event/ngx_event.h  (用户注释 108 行; 丢中文 1 行, 丢英文 2 行)
    - [中] //标志位，为1时表示等待字符流结束，它只与 kqueue和 aio事件驱动机制有关
    - [英] * The flag is delivered via the EPOLLRDHUP epoll event or EV_EOF kqueue flag.
    - [英] * Flag indicating that EOF is pending on the socket, even though there may be some data available before it.

## src/http/modules/ngx_http_autoindex_module.c  (用户注释 87 行; 丢中文 3 行, 丢英文 0 行)
    - [中] //关闭文件夹，返回500错误
    - [中] * 错误，关闭打开的文件目录。返回500
    - [中] //初始化entries动态数组

## src/http/ngx_http_script.c  (用户注释 195 行; 丢中文 3 行, 丢英文 0 行)
    - [中] //执行每个计算长度的code，返回的长度累加，得到整个复杂变量的总长
    - [中] //捕获到的数据
    - [中] //分配可以存储整个复杂变量的内存空间

## src/os/unix/ngx_process_cycle.h  (用户注释 17 行; 丢中文 2 行, 丢英文 1 行)
    - [中] // 打开频道，使用频道这种方式通信前必须发送的命令
    - [中] * master与worker间通信的命令 参考ngx_channel_t
    - [英] /**

## src/core/ngx_output_chain.c  (用户注释 115 行; 丢中文 2 行, 丢英文 0 行)
    - [中] /* 跳过bsize为0的buf */
    - [中] //如果buf的大小为0，并且不是特殊buf, 则跳过

## src/core/ngx_resolver.c  (用户注释 144 行; 丢中文 2 行, 丢英文 0 行)
    - [中] //分配并初始化rn节点
    - [中] /*红黑树中未找到*/

## src/http/modules/ngx_http_log_module.c  (用户注释 176 行; 丢中文 2 行, 丢英文 0 行)
    - [中] //将日志内存赋值到缓冲区
    - [中] //构建日志

## src/http/modules/ngx_http_upstream_keepalive_module.c  (用户注释 80 行; 丢中文 0 行, 丢英文 2 行)
    - [英] *     conf->max_cached = 0;
    - [英] *     conf->original_init_upstream = NULL;

## src/http/ngx_http_script.h  (用户注释 89 行; 丢中文 2 行, 丢英文 0 行)
    - [中] * 获取普通变量执行的指令 $host
    - [中] //表示set指令中的变量名（第一个参数）

## src/http/ngx_http_variables.c  (用户注释 318 行; 丢中文 1 行, 丢英文 0 行)
    - [中] //使用random()随机生成

============================================================
没有丢失任何注释的文件: 123 个
